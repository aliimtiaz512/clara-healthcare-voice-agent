# Clara admin dashboard · client environment
# Copy this file to `.env` and adjust if the API runs elsewhere.

# Base URL of the FastAPI admin API (read by src/lib/api.ts).
NEXT_PUBLIC_API_URL=http://localhost:8000
