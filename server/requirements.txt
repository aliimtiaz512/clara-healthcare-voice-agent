# Clara – Avery Wellness Clinic Voice Agent
# Install with: pip install -r requirements.txt

# ── LiveKit Agents core + plugins ──────────────────────────────────────────
# 1.x ships the modern Agent / AgentSession API used in agent.py.
# Extras pull in the Deepgram (STT + Aura TTS), OpenAI-compatible LLM
# (used here as the Groq shim), and Silero VAD plugins.
livekit-agents[deepgram,openai,silero]>=1.0.0,<2.0.0

# LiveKit token generation (required by api.py for the browser test-call)
livekit-api>=0.8.0

# ── Admin API ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# ── Database ──────────────────────────────────────────────────────────────
# asyncpg is mandatory per arch.md — never use synchronous psycopg2 in voice threads
asyncpg>=0.29.0

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
